import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";
import Footer from "@/components/personal/Footer";
import Navbar from "@/components/personal/Navbar";

// Instead of using a variable font directly, use `link` tag to include fonts
const poppins = Poppins({
  subsets: ["latin"],
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "MineralCat PTE",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        {/* Adding link tag for the font to ensure no hydration mismatch */}
        {/* <link
          href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap"
          rel="stylesheet"
        /> */}
      </head>
      <body className={`antialiased bg-white ${poppins.className}`}>
        {/* header */}
        <Navbar />

        <section className="min-h-dvh">
          {children}
        </section>

        {/* footer */}
        <footer className="flex items-center justify-center mt-10">
          <Footer />
        </footer>
      </body>
    </html>
  );
}
